const SendInput = (props) => {
    return (
        <input className={'w-full h-full py-2 px-4 rounded-full bg-white'} placeholder={'Введите сообщение'} {...props} />
    )
}

export default SendInput